function DatabaseEdit(){
    var test = window.location.href;
    test = test.substring(0, 22);
    test += "/CowData/Index";
    window.location.href = test;
}

function PicLearn() {
    var test = window.location.href;
    test = test.substring(0, 22);
    test += "/CowData/Learn";
    window.location.href = test;
}

function ShowCowVideo() {
    var test = window.location.href;
    test = test.substring(0, 22);
    test += "/Video/vi";
    window.location.href = test;
}